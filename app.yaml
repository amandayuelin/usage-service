name: usage-events-app
services:
  - name: web
    image: registry.digitalocean.com/<registry>/usage-service:latest
    run_command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    envs:
      - key: DATABASE_URL
        value: sqlite:///./data.db
      - key: PORT
        value: "8000"
